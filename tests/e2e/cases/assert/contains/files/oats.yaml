oats-schema-version: 3
name: contains
fixture:
  type: remote
  endpoint: {{REMOTE_OTLP_HTTP}}
seed:
  type: inline-otlp
  logs:
    - service: {{CASE_NAME}}
      body: seed-log-line
expected:
  logs:
    - logql: '{service_name="{{CASE_NAME}}"}'
      contains: seed-log-line
