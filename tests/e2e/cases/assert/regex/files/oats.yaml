oats-schema-version: 3
name: regex
fixture:
  type: remote
  endpoint: {{REMOTE_OTLP_HTTP}}
seed:
  type: inline-otlp
  logs:
    - service: {{CASE_NAME}}
      body: seed-log-line
expected:
  logs:
    - logql: '{service_name="{{CASE_NAME}}"}'
      regex: seed-log-line
