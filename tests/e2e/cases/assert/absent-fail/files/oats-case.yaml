name: absent-fail
fixture:
  remote:
    endpoint: {{REMOTE_OTLP_HTTP}}
seed:
  type: inline-otlp
  logs:
    - service: {{CASE_NAME}}
      body: present-log-line
expected:
  logs:
    - logql: '{service_name="{{CASE_NAME}}"}'
      absent: true
