oats: 2
name: regex-fail
fixture:
  type: remote
  endpoint: {{REMOTE_OTLP_HTTP}}
seed:
  type: inline-otlp
  logs:
    - service: {{CASE_NAME}}
      body: seed-log-line
expected:
  logs:
    - logql: '{service_name="{{CASE_NAME}}"}'
      regex: needle.*
