services:
  # A real instrumented app: telemetrygen exports OTLP traces to the LGTM
  # collector (added by `template: lgtm`). restart: always re-emits until LGTM
  # is ready, so the trace reliably lands regardless of startup ordering.
  telemetrygen:
    image: ghcr.io/open-telemetry/opentelemetry-collector-contrib/telemetrygen:latest@sha256:fd38dc128b69e54b09be3f6c11be550edc8ca8bb77baa21d4ebe3ea110934513
    command:
      - traces
      - --otlp-endpoint
      - lgtm:4317
      - --otlp-insecure
      - --service
      - oats-app-e2e
      - --traces
      - "5"
    restart: always
    depends_on:
      - lgtm
