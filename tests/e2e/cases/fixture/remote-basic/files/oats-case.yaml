name: remote-basic
fixture:
  remote:
    # Placeholder value; tests/e2e/cases/fixture/remote-basic/test.yaml overwrites this file with the dynamically allocated OTLP port.
    endpoint: http://127.0.0.1:4318
seed:
  type: inline-otlp
  logs:
    - service: remote-basic
      body: seed-log-line
expected:
  logs:
    - logql: '{service_name="remote-basic"}'
      match:
        - name: seed-log-line
          attributes:
            - key: service_name
              value: remote-basic
