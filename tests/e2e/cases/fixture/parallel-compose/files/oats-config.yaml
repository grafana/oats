meta:
  version: 3
# alpha and beta declare DIFFERENT compose fixtures (different files), so they
# form two independent boot-groups. With --parallel 2 they boot their own lgtm
# stacks concurrently — which is what the verify-*.sh rendezvous proves.
cases: ["alpha.yaml", "beta.yaml"]
