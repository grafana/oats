oats: 2
name: profile smoke
seed:
  type: app
expected:
  profiles:
    - query: 'process_cpu:cpu:nanoseconds:cpu:nanoseconds{}'
      match:
        - match_type: regexp
          name: 'main|worker'

